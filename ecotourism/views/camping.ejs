<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Кемпінги</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            font-family: 'Poppins', sans-serif;
            text-transform: none;
            box-sizing: border-box;
            transition: all .3s ease;
            text-decoration: none;
        }

        nav {
            width: 100%;
            height: 65px;
            backdrop-filter: blur(1px);
            border-radius: .7rem;
            display: flex;
            justify-content: flex-start;
            align-items: center;
            padding: 0 2rem;
            z-index: 10;
        }

        nav .logo h3 {
            color: var(--white-color);
            font-weight: 400;
            cursor: pointer;
        }
    
        :root {
            --dark-green: #122b1d;
            --green-color: #015743;
            --white-color: #fff;
        }
    
        body {
            background-color: var(--dark-green);
            color: var(--white-color);
            padding: 2rem;
        }
    
        h1 {
            text-align: center;
            font-size: 2rem;
            margin-bottom: 1.5rem;
        }
    
        ul {
            list-style: none;
            padding: 0;
            display: flex;
            flex-wrap: wrap;
            gap: 1.5rem;
            justify-content: center;
        }
    
        li {
            background: var(--white-color);
            color: var(--dark-green);
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            max-width: 280px;
            transition: transform 0.3s;
        }
    
        li:hover {
            transform: translateY(-10px);
        }
    
        li img {
            width: 100%;
            height: 180px;
            object-fit: cover;
        }
    
        h2 {
            font-size: 1.2rem;
            margin: 0.8rem;
        }
    
        p {
            font-size: 0.9rem;
            margin: 0.8rem;
            color: #333;
        }
    
        form {
            display: flex;
            justify-content: center;
            margin-bottom: 1rem;
        }
    
        button {
            background-color: var(--green-color);
            color: var(--white-color);
            padding: 0.4rem 0.8rem;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 0.9rem;
        }
    
        button:hover {
            background-color: #02636e;
        }
    
        a {
            display: block;
            text-align: center;
            color: var(--white-color);
            font-size: 1rem;
            margin-top: 1.5rem;
        }
    
        a:hover {
            color: #aaa;
        }
    </style>    
</head>
<body>
    <nav>
        <div class="logo">
            <a href="/"><h3>Ecotourism 💚 Ukraine</h3></a>
        </div>
    </nav>
    <h1>Список кемпінгів</h1>
    <ul>
        <% campings.forEach(camping => { %>
            <li>
                <img src="/photos/<%= camping.image_url %>" alt="<%= camping.name %>">
                <h2><%= camping.name %></h2>
                <p><%= camping.description %></p>
                <p><b>Рейтинг:</b> <%= camping.rating %> | <%= camping.reviews_count %> відгуків</p>
                <p><b>Ціна:</b> від <%= camping.price_per_night %> грн/ніч</p>
                <form action="/save-favorites" method="POST">
                    <input type="hidden" name="userId" value="1"> 
                    <input type="hidden" name="campingId" value="<%= camping.id %>">
                    <button type="submit">Зберегти до улюблених</button>
                </form>
            </li>
        <% }); %>
    </ul>    
</body>
</html>